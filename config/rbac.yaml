apiVersion: v1
kind: ServiceAccount
metadata:
  name: tap-operator-copy-packages-sa
  namespace: tap-operator
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: tap-operator-copy-packages-role
rules:
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["*"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: tap-operator-copy-packages-role-binding
subjects:
  - kind: ServiceAccount
    name: tap-operator-copy-packages-sa
    namespace: tap-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: tap-operator-copy-packages-role
